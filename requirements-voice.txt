# Voice Specialist dependencies — تُثبَّت فقط لو VOICE_INSTALL=true
# الحجم الكلي ~4-6 GB (torch + whisper + TTS)
#
# تثبيت داخل Container الشغّال (للتجربة السريعة):
#   docker exec yesarha_backend pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   docker exec yesarha_backend pip install openai-whisper TTS
#
# أو عبر Docker build (دائم):
#   docker compose build --build-arg VOICE_INSTALL=true backend

# PyTorch — CUDA 12.1 (RTX 4060)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.12.1
torchaudio==2.3.1+cu121

# Whisper + TTS
openai-whisper==20231117
TTS==0.22.0
