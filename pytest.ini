[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::sqlalchemy.exc.SAWarning
markers =
    ollama: requires a running Ollama instance (deselect with -m "not ollama")
    slow: slow integration tests
