# Core Framework
fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
pydantic[email]
pydantic-settings

# Auth
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
python-multipart

# HTTP
requests
httpx

# Caching & Queue
redis

# System Monitoring
psutil

# Scheduling (weekly scan)
apscheduler

# Voice Specialist (Phase 3)
# تُثبَّت عبر Docker build arg: VOICE_INSTALL=true
# أو يدوياً داخل الـ container:
#   docker exec -it yesarha_backend-backend-1 bash
#   pip install openai-whisper TTS torch torchaudio
# لا تضفها هنا مباشرة — حجمها كبير وتبطئ الـ build

# Streaming (SSE)
sse-starlette

# Rate limiting
slowapi
